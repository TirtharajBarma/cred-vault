Absolutely. Great call.
You’re right: after multiple iterations, the repo likely has both dead code and duplicate logic paths.  
Here is a full cleanup plan (old + latest), focused on making the system simple and maintainable.
Current Read
- You have core flows working, but multiple overlapping paths still exist.
- Biggest cleanup opportunity is not just “unused functions”; it is removing alternate business paths that can drift.
High-Confidence Dead/Redundant Candidates (already identified)
- server/services/card-service/CardService.API/BackgroundServices/OverdueBillCheckService.cs
  - Not registered anymore; file is effectively dead.
- server/services/billing-service/BillingService.API/Controllers/BillsController.cs:81 (POST /{billId}/mark-paid)
  - “admin/testing” path; bypasses normal ownership semantics and has caused confusion.
- server/services/billing-service/BillingService.Application/Commands/Statements/GenerateStatementCommand.cs
  - Likely redundant now that statement is tied to bill generation/payment update flow.
- server/services/payment-service/PaymentService.Application/Commands/Payments/InitiatePaymentCommand.cs:22
  - InitiatePaymentResult still contains OtpCode; no longer needed in API response model.
- server/services/payment-service/PaymentService.API/Controllers/PaymentsController.cs:37
  - isDevelopment local variable is unused.
- Historical DB artifacts (not code, but causes logic noise):
  - CardViolations rows with zero BillId from old overdue path.
---
Detailed Cleanup Plan (End-to-End)
Phase 0: Guardrails (no deletion yet)
- Freeze a baseline:
  - capture current API smoke results for: bill gen, statement list, payment initiate/verify, overdue check, notifications.
- Add “behavior contract” doc:
  - one canonical flow only (bill -> statement -> payment -> rewards-if-tier -> notification).
- Add rollback checkpoints per service.
Phase 1: Build a Dead-Code Inventory
- Generate symbol/reference inventory for:
  - controllers, handlers, consumers, background services, DTOs, events, routes.
- Classify each candidate:
  - Dead (unreachable)
  - Duplicate (second path to same outcome)
  - Legacy (old behavior not desired anymore)
  - Keep (still needed)
- Output artifact: CLEANUP-INVENTORY.md with confidence + owner decision.
Phase 2: Canonicalize Business Flows (important)
- Choose one source of truth for each concern:
  - Bill creation: auto cycle + admin force only.
  - Statement lifecycle: bill-generated creates statement; payment updates same statement.
  - Overdue: billing service overdue job/check only; card acts only on billing event.
  - Payment completion: saga path only.
  - Rewards: only if matching tier; no fallback.
- Any alternate path gets removed or explicitly marked “admin repair only”.
Phase 3: Service-by-Service Cleanup
- Billing Service
  - Decide fate of GenerateStatementCommand path:
    - either remove, or keep as explicit “rebuild/regenerate statement” admin tool only.
  - Remove or hard-restrict mark-paid endpoint (testing bypass).
  - Ensure statement DTO always includes BillId so UI doesn’t need inference.
  - Remove obsolete comments/branches tied to previous statement model.
- Card Service
  - Delete dead OverdueBillCheckService file.
  - Keep only event-driven strike application from IBillOverdueDetected.
  - Remove any leftover logic that infers overdue purely from card balance.
  - Clean old violation interpretation logic if tied to legacy rows.
- Payment Service
  - Remove OtpCode from result objects where no longer consumed.
  - Remove unused locals / stale conditionals.
  - Ensure one identity-fetch strategy only (/users/me for user-token contexts).
  - Keep initiation/verify APIs minimal and stable.
- Notification Service
  - Remove event handling branches not tied to active flows.
  - Keep only current subjects/types used by active saga/billing events.
  - Simplify audit/log filtering paths if duplicates exist.
- Frontend
  - Remove stale API fields/contracts (already started with devOtp).
  - Remove dead UI states around old payment type/card-selection paths.
  - Verify all routes/components are reachable from navbar/user journey; prune orphan admin pages if not used.
Phase 4: Messaging Topology Cleanup
- Build a publish/consume matrix for all contracts:
  - every published event must have at least one intended consumer,
  - every consumer should correspond to a currently emitted event.
- Remove orphan consumers/contracts.
- Keep a single event naming style and payload shape.
Phase 5: Data + Legacy Cleanup
- Create DB cleanup scripts for historical artifacts:
  - invalid CardViolations (e.g., BillId=00000000...) flagged/archived or corrected.
  - old inconsistent saga rows optionally archived.
- Add “legacy-data-safe” guards in query code to avoid legacy rows affecting current behavior.
- Keep scripts idempotent and auditable.
Phase 6: Hard Validation (must pass before merge)
- Scenario tests (manual/curl scripted):
  1) bill generate -> statement exists,
  2) pay with OTP -> bill paid + statement paid + optional reward (only with tier),
  3) overdue check -> overdue + strike event path,
  4) notification logs for OTP/payment.
- Cross-DB correlation check via server/sql/payment-correlation-audit.sql.
- Build checks for all services + client.
Phase 7: Enforce “No Drift” going forward
- Enable stricter analyzers:
  - C#: unused members/locals warnings elevated.
  - TS: noUnusedLocals, noUnusedParameters.
- Add PR checklist:
  - “Introduces alternate path?” must be justified.
  - “Any unused DTO/handler/event?” must be answered.
---
Execution Order I Recommend
1) Inventory + decisions (fast, low risk)  
2) Remove dead files/endpoints (low-medium risk)  
3) Remove duplicate flow code (medium risk)  
4) Data cleanup scripts (medium risk)  
5) Validation run + lock-in lint/analyzers (low risk)
---
Decision Points You Should Confirm Before I Execute
1) GenerateStatementCommand path: remove completely or keep as admin “rebuild statement”?  
2) mark-paid endpoint: remove entirely or keep admin-only with stricter semantics?  
3) Legacy bad CardViolations: soft-archive or hard-delete?
If you confirm these 3, I can convert this into a concrete task-by-task execution plan with file-level actions and rollback steps.















x.AddConsumer<PaymentCompletedConsumer>();
x.AddConsumer<PaymentReversedConsumer>();
/PaymentCompletedConsumer.cs
-63 lines
backgroundservice/OverdueBillCheckService.cs